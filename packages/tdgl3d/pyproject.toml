[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tdgl3d"
version = "2.0.0"
description = "3D Time-Dependent Ginzburg-Landau superconductor vortex simulator"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "Owen Medeiros"},
]
dependencies = [
    "numpy>=1.24",
    "scipy>=1.10",
    "matplotlib>=3.7",
    "h5py>=3.8",
    "tqdm>=4.65",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "ruff>=0.1",
]
# Reference implementations for the cross-tool benchmarks in benchmarks/.
# Not needed to run the solver or its tests.
benchmarks = [
    "tdgl>=0.8",
    "superscreen>=0.10",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"

# Lint config lives in the repo-root ruff.toml.
